export const metadata = {
  title: 'Android Key Tracker',
  description: 'Application de suivi des touches Android en temps réel',
}

export default function RootLayout({ children }) {
  return (
    <html lang="fr">
      <body>{children}</body>
    </html>
  )
}
